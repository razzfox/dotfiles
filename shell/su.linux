# Sets new USER environment variables and cd to user home. Needed esp for aura, who does not look at EUID
su() {
  if test -z "$TMUX"; then
    $(which su) --login "$@"
  else
    $(which su) -p "$@"
  fi
}
